<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppControlManager.Pages.LinkPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppControlManager.Pages"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    FlowDirection="{x:Bind AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Navigation Bar -->
        <controls:WrapPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" HorizontalSpacing="3" VerticalSpacing="3" VerticalAlignment="Top" Margin="0,0,0,4">

            <Button Click="{x:Bind BackButton_Click}" IsEnabled="False" x:Name="BackButton">
                <FontIcon Glyph="&#xE72B;" FontSize="12" />
            </Button>

            <Button Click="{x:Bind ForwardButton_Click}" IsEnabled="False" x:Name="ForwardButton">
                <FontIcon Glyph="&#xE72A;" FontSize="12" />
            </Button>

            <Button Click="{x:Bind ReloadButton_Click}">
                <FontIcon Glyph="&#xE72C;" FontSize="12"/>
            </Button>

            <Button Click="{x:Bind HomeButton_Click}">
                <FontIcon Glyph="&#xE80F;" FontSize="12"/>
            </Button>

            <Button Click="{x:Bind OpenURL_Click}" ToolTipService.ToolTip="Open URL" Style="{StaticResource AccentButtonStyle}">
                <FontIcon Glyph="&#xE71B;" FontSize="12"/>
            </Button>
        </controls:WrapPanel>

        <WebView2 Grid.Row="1"
                  x:Name="LinkPreviewWebView2"
                  NavigationCompleted="{x:Bind WebView2_NavigationCompleted}"
                  DefaultBackgroundColor="Transparent"
                  Source="{x:Bind PreviewUrl, Mode=OneWay}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Visible"
                  ScrollViewer.HorizontalScrollMode="Auto"
                  ScrollViewer.IsHorizontalRailEnabled="True"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  ScrollViewer.VerticalScrollMode="Auto"
                  ScrollViewer.IsVerticalRailEnabled="True" />
    </Grid>
</Page>