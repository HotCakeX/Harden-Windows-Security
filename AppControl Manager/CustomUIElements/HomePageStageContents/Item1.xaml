<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AppControlManager.CustomUIElements.HomePageStageContents.Item1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppControlManager.CustomUIElements.HomePageStageContents">

    <ScrollView>

        <Grid Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                   x:Uid="HelloAndWelcome"
                   TextWrapping="Wrap"
                   FontSize="20"
                   Margin="0,0,0,4"
                   Foreground="#FFFFD28C"/>

            <TextBlock Grid.Row="1"
                   x:Uid="ThanksForChoosingTheApp"
                   FontSize="13"
                   TextWrapping="Wrap"
                   Foreground="#FFF0DCC8"
                   Margin="0,0,0,12"/>

            <Button Grid.Row="2"
                x:Uid="VisitGitHubReleasesButton"
                HorizontalAlignment="Left"
                FontSize="14"
                Click="{x:Bind Button_Click}"
                Padding="14,6"
                Background="#FF8A5A18"
                Foreground="White"
                BorderThickness="0"
                CornerRadius="6"/>
        </Grid>

    </ScrollView>

</UserControl>
