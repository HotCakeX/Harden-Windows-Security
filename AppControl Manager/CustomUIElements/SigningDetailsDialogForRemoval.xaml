<customUI:ContentDialogV2
    x:Class="AppControlManager.CustomUIElements.SigningDetailsDialogForRemoval"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppControlManager.CustomUIElements"
    xmlns:customUI="using:AppControlManager.CustomUIElements"
    xmlns:CCToolKit="using:CommonCore.ToolKits"
    PrimaryButtonClick="{x:Bind OnPrimaryButtonClick}"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    x:Uid="SigningDetailsDialog"
    IsPrimaryButtonEnabled="False"
    DefaultButton="Primary">

    <ScrollView>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock IsTextSelectionEnabled="True"
                       x:Uid="PolicyRemovalDialogTopTextBlock"
                       Grid.Row="0"
                       TextWrapping="Wrap"
                       VerticalAlignment="Top" />

            <InfoBar Margin="15"
                     Grid.Row="1"
                     BorderThickness="0"
                     CornerRadius="0"
                     IsIconVisible="True"
                     IsOpen="True"
                     IsClosable="False"
                     x:Uid="PolicyManagementDialogTopInfoBar"
                     Severity="Informational">
                <InfoBar.ActionButton>
                    <Button x:Uid="GoToSettingsButton" Click="{x:Bind OpenAppSettingsButton_Click}" />
                </InfoBar.ActionButton>
            </InfoBar>

            <StackPanel HorizontalAlignment="Stretch"
                        Spacing="{StaticResource SettingsCardSpacing}"
                        Grid.Row="2">

                <win:StackPanel.ChildrenTransitions>
                    <win:EntranceThemeTransition FromVerticalOffset="50" />
                    <win:RepositionThemeTransition IsStaggeringEnabled="False" />
                </win:StackPanel.ChildrenTransitions>

                <CCToolKit:SettingsCard x:Uid="CertificateDetailsSettingsCard">
                    <CCToolKit:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEA86;"/>
                    </CCToolKit:SettingsCard.HeaderIcon>

                    <CCToolKit:WrapPanel Orientation="Vertical" VerticalSpacing="10" HorizontalSpacing="10">

                        <Button x:Name="CertFileBrowseButton" HorizontalAlignment="Center" Click="{x:Bind CertFileBrowseButton_Click}" x:Uid="FileBrowseButton"/>
                        <TextBox MaxWidth="700" x:Name="CertFilePathTextBox" TextWrapping="Wrap" PlaceholderText=".cer file path" />

                    </CCToolKit:WrapPanel>

                </CCToolKit:SettingsCard>

                <CCToolKit:SettingsCard x:Uid="CertificateCommonNameSettingsCard">
                    <CCToolKit:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEA86;"/>
                    </CCToolKit:SettingsCard.HeaderIcon>

                    <CCToolKit:WrapPanel Orientation="Vertical" HorizontalSpacing="10" VerticalSpacing="10">

                        <AutoSuggestBox x:Name="CertificateCommonNameAutoSuggestBox"
                                        MaxWidth="700"
                                        QueryIcon="Find"
                                        GotFocus="{x:Bind CertificateCommonNameAutoSuggestBox_GotFocus}"
                                        TextChanged="{x:Bind CertificateCNAutoSuggestBox_TextChanged}"
                                        PlaceholderText="Find Certificate Common Names"/>

                    </CCToolKit:WrapPanel>

                </CCToolKit:SettingsCard>

                <CCToolKit:SettingsCard x:Uid="XMLPolicyPathForRemovalSettingsCard">
                    <CCToolKit:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEA86;"/>
                    </CCToolKit:SettingsCard.HeaderIcon>

                    <CCToolKit:WrapPanel VerticalSpacing="10" HorizontalSpacing="10" Orientation="Vertical">

                        <Button x:Name="XMLPolicyFileBrowseButton" Click="{x:Bind XMLPolicyFileBrowseButton_Click}" HorizontalAlignment="Center" x:Uid="FileBrowseButton"/>

                        <TextBox MaxWidth="700" x:Name="XMLPolicyFileTextBox" PlaceholderText="XML file path" TextWrapping="Wrap"/>

                    </CCToolKit:WrapPanel>

                </CCToolKit:SettingsCard>

            </StackPanel>

            <CCToolKit:WrapPanel Grid.Row="3" Orientation="Vertical" HorizontalSpacing="10" VerticalSpacing="10" Margin="10,15,10,0" HorizontalAlignment="Center">

                <Button HorizontalAlignment="Center"
                        ToolTipService.ToolTip="Verify the settings before submitting them"
                        Click="{x:Bind VerifyButton_Click}"
                        Loaded="{x:Bind VerifyButton_Loaded}"
                        x:Name="VerifyButton">

                    <Button.Content>
                        <StackPanel Orientation="Horizontal" Padding="10,5,10,5">
                            <ProgressRing VerticalAlignment="Center" x:Name="VerifyButtonProgressRing" Margin="0,0,10,0" Visibility="Collapsed" IsIndeterminate="True"/>
                            <TextBlock x:Name="VerifyButtonContentTextBlock" VerticalAlignment="Center" FontWeight="SemiBold" Text="Verify"/>
                        </StackPanel>
                    </Button.Content>

                </Button>

                <TeachingTip x:Name="VerifyButtonTeachingTip"
                            Target="{x:Bind VerifyButton}"
                            Title="Error">
                </TeachingTip>

            </CCToolKit:WrapPanel>

        </Grid>

    </ScrollView>

</customUI:ContentDialogV2>
