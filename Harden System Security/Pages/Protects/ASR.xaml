<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HardenSystemSecurity.Pages.Protects.ASR"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:customUI="using:AppControlManager.CustomUIElements"
    xmlns:others="using:AppControlManager.Others"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:viewModels="using:HardenSystemSecurity.ViewModels"
    xmlns:animatedvisuals="using:AnimatedVisuals"
    FlowDirection="{x:Bind ViewModel.AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <!-- Defines a single ASRRuleEntry that is displayed in the ListView -->
            <DataTemplate x:Key="ASRRuleListViewTemplate"
                  x:DataType="viewModels:ASRRuleEntry">
                <Border BorderThickness="1"
                        BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,4,0,4"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Rule Name -->
                        <TextBlock Grid.Row="0"
                                   Text="{x:Bind PolicyEntry.FriendlyName, Mode=OneWay}"
                                   ToolTipService.ToolTip="{x:Bind PolicyEntry.FriendlyName, Mode=OneWay}"
                                   AutomationProperties.HelpText="{x:Bind PolicyEntry.FriendlyName, Mode=OneWay}"
                                   TextWrapping="WrapWholeWords"
                                   x:Phase="1"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Margin="0,0,0,8"/>

                        <!-- Link + Intents row -->
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Link Button -->
                            <customUI:LinkButtonV2
                                Grid.Column="0"
                                LinkUrl="{x:Bind PolicyEntry.URL, Mode=OneWay}"
                                HorizontalAlignment="Left"
                                Visibility="{x:Bind HasURL, Mode=OneWay}"/>

                            <!-- Device Intents circles (auto-collapses when no intents) -->
                            <customUI:IntentCircles
                                Grid.Column="1"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Margin="12,0,0,0"
                                ItemsSource="{x:Bind PolicyEntry.DeviceIntents, Mode=OneWay}"/>
                        </Grid>

                        <!-- State Selection -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="State:"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"
                                       Style="{ThemeResource BodyTextBlockStyle}"/>

                            <ComboBox Grid.Column="1"
                                      SelectedIndex="{x:Bind StateIndex, Mode=TwoWay}"
                                      HorizontalAlignment="Left"
                                      MinWidth="140">
                                <ComboBoxItem Content="Not Configured"/>
                                <ComboBoxItem Content="Block"/>
                                <ComboBoxItem Content="Audit"/>
                                <ComboBoxItem Content="Warn"/>
                            </ComboBox>
                        </Grid>

                        <!-- Apply Button -->
                        <Grid Grid.Row="2">
                            <Button HorizontalAlignment="Right"
                                    Click="{x:Bind ApplyRule}">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                              Glyph="&#xE73E;"
                                              FontSize="14"/>
                                    <TextBlock x:Uid="ApplyText"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                    </Grid>
                </Border>
            </DataTemplate>
        </Grid.Resources>

        <controls:WrapPanel Style="{StaticResource PageHeaderWrapPanelStyle}">

            <TextBlock x:Uid="ASRRulesPageTitle" TextWrapping="WrapWholeWords" Style="{StaticResource BodyTextBlockStyle}" />

            <customUI:GuideButton NavigateUri="https://github.com/HotCakeX/Harden-Windows-Security/wiki/Attack-Surface-Reduction" />

        </controls:WrapPanel>

        <customUI:InfoBarV2 Grid.Row="1"
                      IsOpen="{x:Bind ViewModel.MainInfoBarIsOpen, Mode=TwoWay}"
                      Message="{x:Bind ViewModel.MainInfoBarMessage, Mode=OneWay}"
                      Severity="{x:Bind ViewModel.MainInfoBarSeverity, Mode=OneWay}"
                      IsClosable="{x:Bind ViewModel.MainInfoBarIsClosable, Mode=OneWay}" />

        <Border Grid.Row="2" Margin="0,10,0,10" Style="{StaticResource GridCardStyle}" Padding="8">

            <controls:WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                HorizontalSpacing="10"
                                VerticalSpacing="10">

                <ProgressRing IsActive="True"
                              Visibility="{x:Bind ViewModel.ProgressBarVisibility, Mode=OneWay}"
                              VerticalAlignment="Center" />

                <TextBox MinWidth="200"
                         x:Uid="SearchBoxTextBox"
                         VerticalAlignment="Center"
                         Text="{x:Bind ViewModel.SearchKeyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" />

                <Button IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}" Click="{x:Bind ViewModel.ApplyAllRules}">

                    <StackPanel Orientation="Horizontal" Spacing="4">

                        <AnimatedIcon Height="25" Width="25">
                            <AnimatedIcon.Source>
                                <animatedvisuals:Spinner/>
                            </AnimatedIcon.Source>
                        </AnimatedIcon>

                        <TextBlock x:Uid="ApplyAllButtonText"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                </Button>

                <Button IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}" Click="{x:Bind ViewModel.RemoveAllRules}">

                    <StackPanel Orientation="Horizontal" Spacing="4">

                        <AnimatedIcon Height="25" Width="25">
                            <AnimatedIcon.Source>
                                <animatedvisuals:Caution/>
                            </AnimatedIcon.Source>
                        </AnimatedIcon>

                        <TextBlock x:Uid="RemoveAllButtonText"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                </Button>

                <Button IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}" Click="{x:Bind ViewModel.ApplyRecommended}">

                    <StackPanel Orientation="Horizontal" Spacing="4">

                        <AnimatedIcon Height="25" Width="25">
                            <AnimatedIcon.Source>
                                <animatedvisuals:Trust/>
                            </AnimatedIcon.Source>
                        </AnimatedIcon>

                        <TextBlock x:Uid="ApplyRecommendedButtonText"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                </Button>

                <Button IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}" Click="{x:Bind ViewModel.RetrieveLatest}">

                    <StackPanel Orientation="Horizontal" Spacing="4">

                        <AnimatedIcon Height="25" Width="25">
                            <AnimatedIcon.Source>
                                <animatedvisuals:Activity/>
                            </AnimatedIcon.Source>
                        </AnimatedIcon>

                        <TextBlock x:Uid="RetrieveLatestStatesButton"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                </Button>

            </controls:WrapPanel>
        </Border>

        <RefreshContainer Grid.Row="3"
                  IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                  RefreshRequested="{x:Bind ViewModel.RetrieveLatest}">

            <ListView x:Name="MainListView"
                      CornerRadius="10"
                      BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                      ItemTemplate="{StaticResource ASRRuleListViewTemplate}"
                      ItemsSource="{x:Bind ViewModel.ASRItemsLVBound, Mode=OneWay}"
                      SelectionMode="None">

            </ListView>

        </RefreshContainer>

    </Grid>
</Page>