<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AppControlManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:WindowComponents="using:AppControlManager.WindowComponents"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:AnimatedVisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:animatedvisuals="using:AnimatedVisuals"
    xmlns:customUI="using:AppControlManager.CustomUIElements"
    xmlns:siPolicy="using:AppControlManager.SiPolicy"
    xmlns:vm="using:AppControlManager.ViewModels"
    xmlns:CCToolKit="using:CommonCore.ToolKits"
    xmlns:CC="using:CommonCore.Others"
    SystemBackdrop="{x:Bind ViewModel.SystemBackDropStyle, Mode=OneWay}"
    Title="AppControl Manager">

    <Grid x:Name="RootGrid" FlowDirection="{x:Bind ViewModel.AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}">

        <Grid.RowDefinitions>
            <!-- Titlebar Row -->
            <RowDefinition Height="Auto"/>
            <!-- NavigationView Row -->
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <!-- For the BreadCrumbBar's size -->
            <x:Double x:Key="BreadcrumbBarChevronFontSize">30</x:Double>

            <!-- Theme Shadow effect used on certain elements inside of the grid -->
            <!-- https://learn.microsoft.com/windows/apps/design/layout/depth-shadow -->
            <ThemeShadow x:Name="SharedShadow" />

            <!-- Pastel Gradient for active item border in the ItemsRepeater-->
            <LinearGradientBrush x:Key="PastelActiveBorderBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#B993D6" Offset="0.0" />
                <GradientStop Color="#8CA6DB" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Resources>

        <!-- When using this, we shouldn't set PaneDisplayMode="" property on the navigation element itself or set that property to anything other than Auto, otherwise these dynamic triggers won't work on it. -->
        <!-- Also apparently there are hardcoded minimum width that do not allow for smaller MinWindowWidth to be set for the triggers below and they are overriden when it happens -->

        <!-- VisualStateManager for responsive layout of the NavigationView -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <!-- Very narrow window state -->
                <VisualState x:Name="DefaultState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainNavigation.PaneDisplayMode" Value="Top" />
                    </VisualState.Setters>
                </VisualState>

                <!-- Narrow window state -->
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainNavigation.PaneDisplayMode" Value="LeftCompact" />
                    </VisualState.Setters>
                </VisualState>

                <!-- Wide window state -->
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="400" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainNavigation.PaneDisplayMode" Value="Left" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Titlebar -->
        <!-- https://learn.microsoft.com/windows/apps/develop/title-bar#full-customization-example -->
        <Grid x:Name="AppTitleBar"
              Loaded="{x:Bind SetRegionsForCustomTitleBar}"
              SizeChanged="{x:Bind SetRegionsForCustomTitleBar}"
              Height="48"
              AutomationProperties.LandmarkType="Custom"
              AutomationProperties.LocalizedLandmarkType="Title Bar"
              Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition x:Name="BackButtonColumn" Width="Auto"/>
                <ColumnDefinition x:Name="HamburgerMenuButtonColumn" Width="Auto"/>
                <ColumnDefinition x:Name="IconColumn" Width="Auto"/>
                <ColumnDefinition Width="{x:Bind ViewModel.TitleColumnWidth, Mode=OneWay}" />
                <ColumnDefinition x:Name="LeftDragColumn" Width="*"/>
                <ColumnDefinition x:Name="SearchColumn" Width="3*"/>
                <ColumnDefinition x:Name="RightDragColumn" Width="*" />
                <ColumnDefinition x:Name="AccountColumn" Width="Auto"/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>

            <!-- Back button style, mimics the same NavigationView back button's style. If the style isn't set here, we can use the built-in animated icon for the back button -->
            <Button x:Name="BackButtonTitleBar"
                    x:Uid="BackButtonTitleBar"
                    Style="{StaticResource NavigationBackButtonNormalStyle}"
                    Grid.Column="1"
                    Click="{x:Bind Nav.BackButtonTitleBar_Click}" />

            <!-- Menu button style, mimics the same NavigationView menu (hamburger) button's style. If the style isn't set here, we can use the built-in animated icon for the menu (hamburger) button -->
            <Button VerticalAlignment="Center"
                    VerticalContentAlignment="Center"
                    x:Name="HamburgerMenuButton"
                    x:Uid="HamburgerMenuButton"
                    Grid.Column="2"
                    Style="{StaticResource PaneToggleButtonStyle}"
                    Margin="10,0,0,0"
                    Click="{x:Bind ViewModel.HamburgerMenuButton_Click}" />

            <!-- App icon -->
            <Image x:Name="TitleBarIcon" Source="ms-appx:///Assets/StoreLogo.png" Grid.Column="3"
                   Width="16" Height="16"
                   Margin="8,0,4,0"/>

            <!-- App title bar -->
            <TextBlock x:Name="TitleBarTextBlock"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Grid.Column="4"
                       VerticalAlignment="Center" />

            <!-- Main search area at the top -->
            <!-- Wrapped in a Grid to allow overlaying the accelerator text -->
            <Grid Grid.Column="6" VerticalAlignment="Center" MaxWidth="600">
                <AutoSuggestBox x:Name="TitleBarSearchBox"
                                QueryIcon="Find"
                                HorizontalAlignment="Stretch"
                                AutomationProperties.LandmarkType="Search"
                                ItemsSource="{x:Bind ViewModel.UnifiedSearchBarSuggestionsCollection, Mode=OneWay}"
                                Text="{x:Bind ViewModel.UnifiedSearchBarText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SuggestionChosen="{x:Bind Nav.SearchBox_SuggestionChosen}"
                                x:Uid="MainSearchAutoSuggestBox"
                                Margin="0"
                                UpdateTextOnSelect="False">
                    <AutoSuggestBox.ItemTemplate>
                        <DataTemplate x:DataType="CC:UnifiedSearchBarResult">
                            <Grid ColumnSpacing="12" Padding="6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Round image on the left -->
                                <Border Grid.Column="0"
                                        Width="32"
                                        Height="32"
                                        CornerRadius="16"
                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                    <ContentPresenter Content="{x:Bind Icon}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                                </Border>

                                <!-- Title and subtitle on the right -->
                                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="{x:Bind Title}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind Subtitle}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Opacity="0.8"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </AutoSuggestBox.ItemTemplate>

                    <AutoSuggestBox.KeyboardAccelerators>
                        <KeyboardAccelerator Key="F" Modifiers="Control" Invoked="SearchAccelerator_Invoked" />
                    </AutoSuggestBox.KeyboardAccelerators>

                </AutoSuggestBox>

                <!-- Accelerator overlay -->
                <StackPanel Orientation="Horizontal"
                            Spacing="4"
                            IsHitTestVisible="False"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            Margin="0,0,45,0"
                            Visibility="{x:Bind IsSearchBoxEmpty(TitleBarSearchBox.Text), Mode=OneWay}">

                    <!-- Ctrl Key -->
                    <Border Background="{ThemeResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="5,2"
                            VerticalAlignment="Center">
                        <TextBlock Text="Ctrl"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Plus symbol -->
                    <TextBlock Text="+"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="0,0,0,1"/>

                    <!-- F Key -->
                    <Border Background="{ThemeResource ControlFillColorSecondaryBrush}"
                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="7,2"
                            MinWidth="20"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                        <TextBlock Text="F"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="11"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                </StackPanel>
            </Grid>

            <!-- Sidebar access point -->
            <Button x:Name="SidebarButton"
                    x:Uid="SidebarButton"
                    Click="{x:Bind ViewModel.SidebarButton_Click}"
                    Grid.Column="8"
                    Height="32"
                    Margin="5,0,30,0"
                    Background="Transparent"
                    BorderThickness="0">

                <Button.Content>
                    <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="7">

                        <AnimatedIcon Height="35" Width="35" Margin="-9,-9,-9,-9">
                            <AnimatedIcon.Source>
                                <animatedvisuals:Sidebar/>
                            </AnimatedIcon.Source>
                        </AnimatedIcon>

                        <InfoBadge Value="{x:Bind ViewModel.SidebarPoliciesLibrary.Count, Mode=OneWay}"
                                   Style="{ThemeResource AttentionIconInfoBadgeStyle}">
                            <InfoBadge.IconSource>
                                <FontIconSource Glyph="&#xEA6A;"/>
                            </InfoBadge.IconSource>
                        </InfoBadge>

                    </controls:WrapPanel>
                </Button.Content>

            </Button>

        </Grid>

        <!-- Navigation View -->
        <NavigationView x:Name="MainNavigation"
                        FlowDirection="{x:Bind ViewModel.AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}"
                        IsTabStop="True"
                        IsPaneOpen="{x:Bind ViewModel.MainNavigationIsPaneOpen, Mode=TwoWay}"
                        ItemInvoked="{x:Bind Nav.MainNavigation_ItemInvoked}"
                        IsBackButtonVisible="Collapsed"
                        IsBackEnabled="False"
                        AlwaysShowHeader="False"
                        AutomationProperties.LandmarkType="Navigation"
                        Grid.Row="1"
                        Margin="{x:Bind ViewModel.NavViewMargin, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.NavViewSelectedItem, Mode=TwoWay}"
                        IsTitleBarAutoPaddingEnabled="False"
                        IsPaneToggleButtonVisible="False">

            <!--
             Being controlled from MainWindow.xaml.cs

            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
            </NavigationView.Resources>

            -->

            <!--
            Used in the Titlebar

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox x:Name="SearchBox"
                        QueryIcon="Find"
                        TextChanged="SearchBox_TextChanged"
                        SuggestionChosen="SearchBox_SuggestionChosen"
                        PlaceholderText="Search menu items..."/>
            </NavigationView.AutoSuggestBox>

           -->

            <!--
            <NavigationView.PaneCustomContent>
                <HyperlinkButton x:Name="PaneHyperlink" Content="More info" Margin="12,0" Visibility="Visible" />
            </NavigationView.PaneCustomContent>
            -->

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeNavItem" x:Uid="HomeNavItem" Icon="{x:Bind ViewModel.HomeIcon, Mode=OneWay}" Tag="Home" />

                <!-- Creation Group -->
                <NavigationViewItemHeader x:Name="CreationNavigationViewItemHeader" x:Uid="CreationNavigationViewItemHeader" />
                <NavigationViewItem x:Name="CreatePolicyNavItem" x:Uid="CreatePolicyNavItem" Icon="{x:Bind ViewModel.CreatePolicyIcon, Mode=OneWay}" Tag="CreatePolicy" />
                <NavigationViewItem x:Name="CreateSupplementalPolicyNavItem" x:Uid="CreateSupplementalPolicyNavItem" Icon="{x:Bind ViewModel.CreateSupplementalPolicyIcon, Mode=OneWay}" Tag="CreateSupplementalPolicy" />
                <NavigationViewItem x:Name="CreateDenyPolicyNavItem" x:Uid="CreateDenyPolicyNavItem" Icon="{x:Bind ViewModel.CreateDenyPolicyIcon, Mode=OneWay}" Tag="CreateDenyPolicy" />

                <!-- Certificates Group -->
                <NavigationViewItemHeader x:Name="CertificatesNavigationViewItemHeader" x:Uid="CertificatesNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="BuildNewCertificateNavItem" x:Uid="BuildNewCertificateNavItem" Icon="{x:Bind ViewModel.BuildNewCertificateIcon, Mode=OneWay}" Tag="BuildNewCertificate" />
                <NavigationViewItem x:Name="ViewFileCertificatesNavItem" x:Uid="ViewFileCertificatesNavItem" Icon="{x:Bind ViewModel.ViewFileCertificatesIcon, Mode=OneWay}" Tag="ViewFileCertificates" />

                <!-- Logs Processing Group -->
                <NavigationViewItemHeader x:Name="LogsProcessingNavigationViewItemHeader" x:Uid="LogsProcessingNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="CreatePolicyFromEventLogsNavItem" x:Uid="CreatePolicyFromEventLogsNavItem" Icon="{x:Bind ViewModel.CreatePolicyFromEventLogsIcon, Mode=OneWay}" Tag="CreatePolicyFromEventLogs" />
                <NavigationViewItem x:Name="CreatePolicyFromMDEAHNavItem" x:Uid="CreatePolicyFromMDEAHNavItem" Icon="{x:Bind ViewModel.CreatePolicyFromMDEAHIcon, Mode=OneWay}" Tag="CreatePolicyFromMDEAH" />

                <!-- Tactical Group -->
                <NavigationViewItemHeader x:Name="TacticalNavigationViewItemHeader" x:Uid="TacticalNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="AllowNewAppsNavItem" x:Uid="AllowNewAppsNavItem" Icon="{x:Bind ViewModel.AllowNewAppsIcon, Mode=OneWay}" Tag="AllowNewApps" />
                <NavigationViewItem x:Name="PolicyEditorNavItem" x:Uid="PolicyEditorNavItem" Icon="{x:Bind ViewModel.PolicyEditorIcon, Mode=OneWay}" Tag="PolicyEditor" />
                <NavigationViewItem x:Name="SimulationNavItem" x:Uid="SimulationNavItem" Icon="{x:Bind ViewModel.SimulationIcon, Mode=OneWay}" Tag="Simulation" />
                <NavigationViewItem x:Name="FirewallSentinelNavItem" x:Uid="FirewallSentinelNavItem" Icon="{x:Bind ViewModel.FirewallSentinelIcon, Mode=OneWay}" Tag="FirewallSentinel" />

                <!-- Info Gathering Group -->
                <NavigationViewItemHeader x:Name="InfoGatheringNavigationViewItemHeader" x:Uid="InfoGatheringNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="SystemInformationNavItem" x:Uid="SystemInformationNavItem" Icon="{x:Bind ViewModel.SystemInformationIcon, Mode=OneWay}" Tag="SystemInformation" />
                <NavigationViewItem x:Name="GetCodeIntegrityHashesNavItem" x:Uid="GetCodeIntegrityHashesNavItem" Icon="{x:Bind ViewModel.GetCodeIntegrityHashesIcon, Mode=OneWay}" Tag="GetCodeIntegrityHashes" />
                <NavigationViewItem x:Name="GetSecurePolicySettingsNavItem" x:Uid="GetSecurePolicySettingsNavItem" Icon="{x:Bind ViewModel.GetSecurePolicySettingsIcon, Mode=OneWay}" Tag="GetSecurePolicySettings" />

                <!-- Policy Management Group -->
                <NavigationViewItemHeader x:Name="PolicyManagementNavigationViewItemHeader" x:Uid="PolicyManagementNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="ConfigurePolicyRuleOptionsNavItem" x:Uid="ConfigurePolicyRuleOptionsNavItem" Icon="{x:Bind ViewModel.ConfigurePolicyRuleOptionsIcon, Mode=OneWay}" Tag="ConfigurePolicyRuleOptions" />
                <NavigationViewItem x:Name="MergePoliciesNavItem" x:Uid="MergePoliciesNavItem" Icon="{x:Bind ViewModel.MergePoliciesIcon, Mode=OneWay}" Tag="MergePolicies" />
                <NavigationViewItem x:Name="DeploymentNavItem" x:Uid="DeploymentNavItem" Icon="{x:Bind ViewModel.DeploymentIcon, Mode=OneWay}" Tag="Deployment" />
                <NavigationViewItem x:Name="ValidatePoliciesNavItem" x:Uid="ValidatePoliciesNavItem" Icon="{x:Bind ViewModel.ValidatePoliciesIcon, Mode=OneWay}" Tag="ValidatePolicies" />

                <!-- Documentation Group -->
                <NavigationViewItemHeader x:Name="DocumentationNavigationViewItemHeader" x:Uid="DocumentationNavigationViewItemHeader"/>
                <NavigationViewItem x:Name="GitHubDocsNavItem" x:Uid="GitHubDocsNavItem" Icon="{x:Bind ViewModel.GitHubDocsIcon, Mode=OneWay}" Tag="GitHubDocs" />
                <NavigationViewItem x:Name="MSFTDocsNavItem" x:Uid="MSFTDocsNavItem" Icon="{x:Bind ViewModel.MSFTDocsIcon, Mode=OneWay}" Tag="MSFTDocs" />

                <NavigationViewItemSeparator/>

                <NavigationViewItem x:Name="LogsNavItem" x:Uid="LogsNavItem" Icon="{x:Bind ViewModel.LogsIcon, Mode=OneWay}" Tag="Logs" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="UpdateNavItem" x:Uid="UpdateNavItem" Icon="{x:Bind ViewModel.UpdateIcon, Mode=OneWay}" Tag="Update">
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge Opacity="{x:Bind ViewModel.InfoBadgeOpacity, Mode=OneWay}"
                                   Style="{ThemeResource AttentionIconInfoBadgeStyle}">
                            <InfoBadge.IconSource>
                                <FontIconSource Glyph="&#xEA6A;"/>
                            </InfoBadge.IconSource>
                        </InfoBadge>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <!-- Split view to create the sidebar -->
            <SplitView x:Name="SidebarSplitView"
                       PaneBackground="Transparent"
                       IsPaneOpen="{x:Bind ViewModel.SidebarPaneIsOpen, Mode=TwoWay}"
                       OpenPaneLength="340"
                       CompactPaneLength="150"
                       DisplayMode="Inline"
                       PanePlacement="Right"
                       AutomationProperties.LandmarkType="Custom"
                       AutomationProperties.LocalizedLandmarkType="Sidebar">

                <SplitView.Pane>
                    <!-- Wrap pane content in a Grid so we can overlay a left-edge sizer -->
                    <Grid>
                        <Border BorderThickness="1"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                CornerRadius="5">

                            <ScrollViewer>

                                <StackPanel Spacing="20" Orientation="Vertical" Margin="10,0,10,0" HorizontalAlignment="Stretch">

                                    <!-- Sidebar TextBlocks and Buttons -->
                                    <TextBlock x:Name="SidebarPoliciesLibraryTextBlock" x:Uid="SidebarPoliciesLibraryTextBlock" Style="{ThemeResource TitleTextBlockStyle}" HorizontalAlignment="Center" Margin="0,15,0,0" HorizontalTextAlignment="Center"/>

                                    <TextBlock x:Name="SidebarMainCaptionTextBlock" x:Uid="SidebarMainCaptionTextBlock" Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Center" HorizontalTextAlignment="Center"/>

                                    <customUI:InfoBarV2 IsOpen="{x:Bind ViewModel.MainInfoBarIsOpen, Mode=TwoWay}"
                                                        Message="{x:Bind ViewModel.MainInfoBarMessage, Mode=OneWay}"
                                                        Severity="{x:Bind ViewModel.MainInfoBarSeverity, Mode=OneWay}"
                                                        IsClosable="{x:Bind ViewModel.MainInfoBarIsClosable, Mode=OneWay}" />

                                    <Border Translation="{x:Bind ViewModel.BorderTranslation, Mode=OneWay}"
                                            BorderThickness="1"
                                            CornerRadius="15"
                                            Shadow="{StaticResource SharedShadow}"
                                            AllowDrop="{x:Bind ViewModel.IsNotElevated}"
                                            DragOver="{x:Bind OnSidebarDragOver}"
                                            Drop="{x:Bind OnSidebarDrop}">

                                        <Border.BorderBrush>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="HotPink" Offset="0" />
                                                <GradientStop Color="Wheat" Offset="1" />
                                            </LinearGradientBrush>
                                        </Border.BorderBrush>

                                        <StackPanel Spacing="10" Orientation="Vertical" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Center">

                                            <TextBox x:Uid="SearchBoxTextBox"
                                                     Margin="10,15,10,0"
                                                     Text="{x:Bind ViewModel.SidebarSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <TextBox.Header>
                                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                                        <FontIcon Glyph="&#xE721;" FontSize="14" />
                                                        <TextBlock x:Uid="SearchThePoliciesLibrary" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </TextBox.Header>
                                            </TextBox>

                                            <ScrollViewer x:Name="Animated_ScrollViewer"
                                                          Height="350"
                                                          ViewChanged="{x:Bind Animated_ScrollViewer_ViewChanged}">

                                                <!--
                                                    Wraping the ItemsRepeater in a Grid with a MinHeight matching the ScrollViewer
                                                    which allows us to center the ItemsRepeater vertically when it has few items.
                                                -->
                                                <Grid MinHeight="350">
                                                    <ItemsRepeater x:Name="animatedScrollRepeater"
                                                                   VerticalAlignment="Center"
                                                                   Margin="10,0,10,0"
                                                                   Visibility="{x:Bind ViewModel.SidebarRepeaterVisibility, Mode=OneWay}"
                                                                   ItemsSource="{x:Bind ViewModel.FilteredSidebarPolicies}"
                                                                   ElementPrepared="{x:Bind OnElementPrepared}">
                                                        <DataTemplate x:DataType="siPolicy:PolicyFileRepresent">

                                                            <!-- SwipeControl is Root so input won't be blocked when content is Z-translated. -->
                                                            <SwipeControl Height="100" Margin="0,4,0,4">

                                                                <SwipeControl.LeftItems>
                                                                    <SwipeItems Mode="Reveal">
                                                                        <SwipeItem x:Uid="SaveAsCIPMenuFlyoutItem" Invoked="OnSwipeSaveAsCIP" CommandParameter="{x:Bind}" Background="Yellow" Foreground="Black">
                                                                            <SwipeItem.IconSource>
                                                                                <FontIconSource Glyph="&#xE74E;" />
                                                                            </SwipeItem.IconSource>
                                                                        </SwipeItem>
                                                                        <SwipeItem x:Uid="SaveAsXMLMenuFlyoutItem" Invoked="OnSwipeSaveAsXML" CommandParameter="{x:Bind}" Background="LightGreen" Foreground="Black">
                                                                            <SwipeItem.IconSource>
                                                                                <FontIconSource Glyph="&#xE78C;" />
                                                                            </SwipeItem.IconSource>
                                                                        </SwipeItem>
                                                                    </SwipeItems>
                                                                </SwipeControl.LeftItems>

                                                                <SwipeControl.RightItems>
                                                                    <SwipeItems Mode="Reveal">
                                                                        <SwipeItem x:Uid="OpenInPolicyEditorMenuFlyoutItem" Invoked="OnSwipeOpenInPolicyEditor" CommandParameter="{x:Bind}" Background="LightBlue" Foreground="Black">
                                                                            <SwipeItem.IconSource>
                                                                                <FontIconSource Glyph="&#xE70F;" />
                                                                            </SwipeItem.IconSource>
                                                                        </SwipeItem>
                                                                        <SwipeItem x:Uid="RemovePolicyFlyoutItem" Invoked="OnSwipeRemove" CommandParameter="{x:Bind}" Background="Red" Foreground="Black">
                                                                            <SwipeItem.IconSource>
                                                                                <FontIconSource Glyph="&#xE74D;" />
                                                                            </SwipeItem.IconSource>
                                                                        </SwipeItem>
                                                                    </SwipeItems>
                                                                </SwipeControl.RightItems>

                                                                <!-- The Elevated Content Container -->
                                                                <Grid x:Name="ItemRoot" CornerRadius="8" Shadow="{StaticResource SharedShadow}">

                                                                    <Grid.ContextFlyout>
                                                                        <MenuFlyout>

                                                                            <MenuFlyoutItem x:Uid="CopyPolicyIDMenuFlyoutItem" Click="OnCopyPolicyID" DataContext="{x:Bind}">
                                                                                <MenuFlyoutItem.Icon>
                                                                                    <FontIcon Glyph="&#xE8C8;" />
                                                                                </MenuFlyoutItem.Icon>
                                                                            </MenuFlyoutItem>

                                                                            <MenuFlyoutItem x:Uid="CopyBasePolicyIDMenuFlyoutItem" Click="OnCopyBasePolicyID" DataContext="{x:Bind}">
                                                                                <MenuFlyoutItem.Icon>
                                                                                    <FontIcon Glyph="&#xE8C8;" />
                                                                                </MenuFlyoutItem.Icon>
                                                                            </MenuFlyoutItem>

                                                                        </MenuFlyout>
                                                                    </Grid.ContextFlyout>

                                                                    <!-- The Interactive Button -->
                                                                    <Button HorizontalAlignment="Stretch"
                                                                            VerticalAlignment="Stretch"
                                                                            HorizontalContentAlignment="Stretch"
                                                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                                            Click="OnAnimatedItemClicked"
                                                                            GotFocus="OnAnimatedItemGotFocus"
                                                                            Padding="10,5"
                                                                            BorderThickness="0">

                                                                        <Button.Flyout>
                                                                            <MenuFlyout>
                                                                                <MenuFlyout.SystemBackdrop>
                                                                                    <MicaBackdrop Kind="Base"/>
                                                                                </MenuFlyout.SystemBackdrop>

                                                                                <MenuFlyoutItem x:Uid="SaveAsXMLMenuFlyoutItem" Click="OnSaveAsXMLClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xE78C;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutItem x:Uid="SaveAsCIPMenuFlyoutItem" Click="OnSaveAsCIPClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xE74E;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutItem x:Uid="RemovePolicyFlyoutItem" Click="OnRemoveClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xE74D;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutItem x:Uid="OpenInPolicyEditorMenuFlyoutItem" Click="OnOpenInPolicyEditorClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xE70F;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutItem x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem" Click="OnConfigureRuleOptionsClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xEEA3;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutItem x:Uid="DeployPolicyFlyoutItem" IsEnabled="{x:Bind IsElevated}" Click="OnDeployClicked" DataContext="{x:Bind}">
                                                                                    <MenuFlyoutItem.Icon>
                                                                                        <FontIcon Glyph="&#xE896;" />
                                                                                    </MenuFlyoutItem.Icon>
                                                                                </MenuFlyoutItem>

                                                                                <MenuFlyoutSubItem x:Name="SidebarPolicyAssignmentMenuFlyoutSubItem"
                                                                                                   x:Uid="SidebarPolicyAssignmentMenuFlyoutSubItem">
                                                                                    <MenuFlyoutSubItem.Icon>
                                                                                        <AnimatedIcon Height="20" Margin="0,0,5,0" Width="20" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarBasePolicySelectButtonLightAnimatedIconVisibility, Mode=OneWay}">
                                                                                            <AnimatedIcon.Source>
                                                                                                <animatedvisuals:Light/>
                                                                                            </AnimatedIcon.Source>
                                                                                        </AnimatedIcon>
                                                                                    </MenuFlyoutSubItem.Icon>

                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect1Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect1Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect1Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect2Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect2Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect2Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect3Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect3Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect3Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect4Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect4Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect4Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect5Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect5Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect5Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect6Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect6Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect6Command, Mode=OneWay}" CommandParameter="{x:Bind}" />
                                                                                    <MenuFlyoutItem HorizontalAlignment="Stretch" Text="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect7Content, Mode=OneWay}" Visibility="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect7Visibility, Mode=OneWay}" Command="{x:Bind vm:ViewModelProvider.SidebarVM.SidebarPolicyConnect7Command, Mode=OneWay}" CommandParameter="{x:Bind}" />

                                                                                </MenuFlyoutSubItem>
                                                                            </MenuFlyout>
                                                                        </Button.Flyout>

                                                                        <StackPanel Orientation="Vertical" Spacing="2" HorizontalAlignment="Center">
                                                                            <Grid ColumnSpacing="5" HorizontalAlignment="Center">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <FontIcon Glyph="&#xEB51;" FontSize="14" VerticalAlignment="Center" />
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{x:Bind PolicyIdentifier}"
                                                                                           TextTrimming="CharacterEllipsis"
                                                                                           TextWrapping="NoWrap"
                                                                                           VerticalAlignment="Center"
                                                                                           Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                                                            </Grid>

                                                                            <Grid ColumnSpacing="5" HorizontalAlignment="Center">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <FontIcon Glyph="&#xE97C;" FontSize="12" VerticalAlignment="Center" />
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{x:Bind PolicyObj.PolicyType}"
                                                                                           TextTrimming="CharacterEllipsis"
                                                                                           TextWrapping="NoWrap"
                                                                                           VerticalAlignment="Center"
                                                                                           Style="{ThemeResource CaptionTextBlockStyle}"/>
                                                                            </Grid>

                                                                            <Grid ColumnSpacing="5" HorizontalAlignment="Center">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <FontIcon Glyph="&#xEF3F;" FontSize="12" VerticalAlignment="Center" />
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{x:Bind SigningStatus}"
                                                                                           TextTrimming="CharacterEllipsis"
                                                                                           TextWrapping="NoWrap"
                                                                                           VerticalAlignment="Center"
                                                                                           Style="{ThemeResource CaptionTextBlockStyle}"/>
                                                                            </Grid>

                                                                            <Grid ColumnSpacing="5" HorizontalAlignment="Center">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <FontIcon Glyph="&#xE765;" FontSize="12" VerticalAlignment="Center" />
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{x:Bind PolicyObj.VersionEx}"
                                                                                           TextTrimming="CharacterEllipsis"
                                                                                           TextWrapping="NoWrap"
                                                                                           VerticalAlignment="Center"
                                                                                           Style="{ThemeResource CaptionTextBlockStyle}"/>
                                                                            </Grid>

                                                                            <Grid ColumnSpacing="5" HorizontalAlignment="Center">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <FontIcon Glyph="&#xE932;" FontSize="12" VerticalAlignment="Center" />
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{x:Bind PolicyID}"
                                                                                           TextTrimming="CharacterEllipsis"
                                                                                           TextWrapping="NoWrap"
                                                                                           VerticalAlignment="Center"
                                                                                           Style="{ThemeResource CaptionTextBlockStyle}"/>
                                                                            </Grid>
                                                                        </StackPanel>
                                                                    </Button>

                                                                    <!-- The Highlight Border (Overlay) direct sibling to button, inside the elevated grid. -->
                                                                    <Border x:Name="HighlightBorder"
                                                                            IsHitTestVisible="False"
                                                                            BorderThickness="2"
                                                                            CornerRadius="8"
                                                                            Opacity="0"
                                                                            BorderBrush="{StaticResource PastelActiveBorderBrush}"/>
                                                                </Grid>
                                                            </SwipeControl>

                                                        </DataTemplate>
                                                    </ItemsRepeater>

                                                    <!--
                                                        Shimmer placeholder for when the list is empty.
                                                    -->
                                                    <StackPanel VerticalAlignment="Center"
                                                                Margin="20,0,20,0"
                                                                Spacing="8"
                                                                Visibility="{x:Bind ViewModel.SidebarShimmerVisibility, Mode=OneWay}">

                                                        <Grid Height="110" CornerRadius="8">
                                                            <CCToolKit:Shimmer />
                                                        </Grid>
                                                        <Grid Height="110" CornerRadius="8">
                                                            <CCToolKit:Shimmer />
                                                        </Grid>
                                                        <Grid Height="110" CornerRadius="8">
                                                            <CCToolKit:Shimmer />
                                                        </Grid>
                                                    </StackPanel>

                                                </Grid>
                                            </ScrollViewer>

                                            <Button x:Uid="ExtraActionsDropDownButton"
                                                    HorizontalAlignment="Center">
                                                <Button.ContentTemplate>
                                                    <DataTemplate x:DataType="x:String">
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <FontIcon Glyph="&#xE712;" FontSize="16"/>
                                                            <TextBlock Text="{x:Bind}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </Button.ContentTemplate>

                                                <Button.Flyout>
                                                    <MenuFlyout>
                                                        <MenuFlyout.SystemBackdrop>
                                                            <MicaBackdrop Kind="Base"/>
                                                        </MenuFlyout.SystemBackdrop>

                                                        <MenuFlyoutItem x:Uid="AddPoliciesMenuFlyoutSubItem" Click="{x:Bind Nav.SidebarBasePolicyBrowseButton_Click}">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE78C;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="RemoveAllMenuFlyoutSubItem" Click="ClearPoliciesLibrary">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE70F;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="BackupAllMenuFlyoutSubItem" Click="BackupLibrary">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xEDA2;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="OpenPoliciesLibraryCacheLocationMenuFlyoutSubItem" Click="OpenPoliciesLibraryCacheLocation">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE8DA;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                    </MenuFlyout>
                                                </Button.Flyout>
                                            </Button>

                                        </StackPanel>

                                    </Border>

                                    <customUI:SettingsCardV2 HorizontalContentAlignment="Center"
                                                             x:Name="PersistentPoliciesLibrarySettingsCard"
                                                             x:Uid="PersistentPoliciesLibrarySettingsCard">
                                        <customUI:SettingsCardV2.HeaderIcon>
                                            <FontIcon Glyph="&#xF32A;"/>
                                        </customUI:SettingsCardV2.HeaderIcon>
                                        <ToggleSwitch x:Uid="ToggleSwitchGeneral"
                                                      x:Name="PersistentPoliciesLibraryToggleSwitch"
                                                      IsOn="{x:Bind ViewModel.AppSettings.PersistentPoliciesLibrary, Mode=TwoWay}"/>
                                    </customUI:SettingsCardV2>

                                    <customUI:SettingsCardV2 HorizontalContentAlignment="Center"
                                                             x:Name="PersistentPoliciesLibraryEncryptionSettingsCard"
                                                             x:Uid="PersistentPoliciesLibraryEncryptionSettingsCard"
                                                             IsEnabled="{x:Bind ViewModel.AppSettings.PersistentPoliciesLibrary, Mode=TwoWay}">
                                        <customUI:SettingsCardV2.HeaderIcon>
                                            <FontIcon Glyph="&#xEC19;"/>
                                        </customUI:SettingsCardV2.HeaderIcon>

                                        <ToggleSwitch x:Uid="ToggleSwitchGeneral"
                                                      HorizontalAlignment="Center"
                                                      x:Name="PersistentPoliciesLibraryEncryptionToggleSwitch"
                                                      IsOn="{x:Bind ViewModel.AppSettings.EncryptPoliciesLibrary, Mode=TwoWay}"/>

                                    </customUI:SettingsCardV2>

                                    <customUI:SettingsCardV2 HorizontalContentAlignment="Center"
                                                             x:Name="PersistentPoliciesLibraryEncryptionScopeSettingsCard"
                                                             x:Uid="PersistentPoliciesLibraryEncryptionScopeSettingsCard"
                                                             IsEnabled="{x:Bind ViewModel.AppSettings.PersistentPoliciesLibrary, Mode=TwoWay}">
                                        <customUI:SettingsCardV2.HeaderIcon>
                                            <FontIcon Glyph="&#xE748;"/>
                                        </customUI:SettingsCardV2.HeaderIcon>

                                        <ToggleSwitch x:Uid="PersistentPoliciesLibraryEncryptionScopeToggleSwitch"
                                                      HorizontalAlignment="Center"
                                                      x:Name="PersistentPoliciesLibraryEncryptionScopeToggleSwitch"
                                                      IsOn="{x:Bind ViewModel.AppSettings.EncryptionScopePoliciesLibrary, Mode=TwoWay}"/>

                                    </customUI:SettingsCardV2>

                                    <HyperlinkButton x:Name="SidebarGuideHyperlinkButton"
                                                     x:Uid="SidebarGuideHyperlinkButton"
                                                     HorizontalAlignment="Center"
                                                     NavigateUri="https://github.com/HotCakeX/Harden-Windows-Security/wiki/Sidebar"
                                                     Padding="12,8"
                                                     CornerRadius="6"
                                                     Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                     BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                     BorderThickness="1"/>

                                    <HyperlinkButton x:Name="SidebarHelpHyperlinkButton"
                                                      HorizontalAlignment="Center"
                                                      NavigateUri="https://github.com/HotCakeX/Harden-Windows-Security/discussions"
                                                      Padding="12,8"
                                                      CornerRadius="6"
                                                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                      BorderThickness="1">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE9CE;"
                                                      Foreground="Gold"
                                                      FontSize="16"/>
                                            <TextBlock x:Uid="SidebarHelpHyperlinkTextBlock"
                                           x:Name="SidebarHelpHyperlinkTextBlock"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </HyperlinkButton>

                                    <Button HorizontalAlignment="Center"
                                            x:Name="OpenConfigDirectoryButton"
                                            x:Uid="OpenConfigDirectoryButton"
                                            IsEnabled="{x:Bind ViewModel.OpenConfigDirectoryButtonState, Mode=OneWay}"
                                            Click="{x:Bind ViewModel.OpenConfigDirectoryButton_Click}">
                                        <Button.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center">
                                                    <FontIcon Glyph="&#xE8A7;" Margin="0,0,8,0" />
                                                    <TextBlock x:Name="OpenConfigDirectoryButtonText"
                                                               x:Uid="OpenConfigDirectoryButtonText"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Button.Content>
                                    </Button>

                                    <Button HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            x:Uid="OptimizeMemoryButton"
                                            IsEnabled="{x:Bind ViewModel.OptimizeMemoryButtonIsEnabled, Mode=OneWay}"
                                            Click="{x:Bind ViewModel.OptimizeMemory}">
                                        <Button.ContentTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <FontIcon Glyph="&#xEEA0;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                                    <TextBlock Text="{x:Bind}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </Button.ContentTemplate>
                                    </Button>

                                </StackPanel>

                            </ScrollViewer>

                        </Border>

                        <!-- Invisible sizer along the left edge of the right-hand pane -->
                        <CCToolKit:PropertySizer Width="8"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Stretch"
                                    Binding="{x:Bind SidebarSplitView.OpenPaneLength, Mode=TwoWay}"
                                    Orientation="Vertical"
                                    IsDragInverted="True"
                                    Background="Transparent"
                                    Minimum="200"
                                    Maximum="1200"
                                    IsTabStop="False"
                                    Margin="0"/>
                    </Grid>
                </SplitView.Pane>

                <!-- The area where the main window content are displayed -->
                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <!-- Overlay Canvas for Animations, must be on top.
                         FlowDirection="LeftToRight" ensures coordinates are always Top-Left origin
                         Opacity="1" here is crucial. We manage visibility via Composition Opacity in code behind -->
                    <Canvas x:Name="AnimationOverlay" Grid.RowSpan="3" IsHitTestVisible="False" ZIndex="999" FlowDirection="LeftToRight">
                        <Image x:Name="TransferIcon" Source="ms-appx:///Assets/External/file.png" Width="40" />
                    </Canvas>

                    <!-- Breadcrumb + header region -->
                    <Grid Grid.Row="0" x:Name="BreadcrumbRegionGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- BreadCrumbBar -->
                        <BreadcrumbBar Grid.Row="0"
                                       Grid.Column="0"
                                       x:Name="MainBreadcrumbBar"
                                       Visibility="{x:Bind ViewModel.IsCrumbBarVisible, Mode=OneWay}"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Top"
                                       Padding="10"
                                       AutomationProperties.LandmarkType="Custom"
                                       AutomationProperties.LocalizedLandmarkType="Bread Crumb Bar Navigation and Page Title"
                                       Margin="20,10,0,0"
                                       ItemsSource="{x:Bind ViewModel.Breadcrumbs}"
                                       ItemClicked="{x:Bind Nav.BreadcrumbBar_ItemClicked}">
                            <BreadcrumbBar.ItemTemplate>
                                <DataTemplate x:DataType="WindowComponents:Crumb"
                                              xmlns:vms="using:AppControlManager.ViewModels">
                                    <TextBlock Text="{x:Bind Label}"
                                               Style="{x:Bind vms:ViewModelProvider.MainWindowVM.BreadcrumbItemStyle, Mode=OneWay}"
                                               TextWrapping="NoWrap"/>
                                </DataTemplate>
                            </BreadcrumbBar.ItemTemplate>
                        </BreadcrumbBar>

                        <!-- Header chevron flyout -->
                        <Button Grid.Row="0"
                                Grid.Column="1"
                                x:Name="RulesHeaderFlyoutButton"
                                Margin="0,10,16,5"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="{x:Bind ViewModel.HeaderFlyoutVisibility, Mode=OneWay}"
                                ToolTipService.ToolTip="Attack Surface Reduction">
                            <Button.Content>
                                <FontIcon Glyph="&#xE70D;" />
                            </Button.Content>
                            <Button.Flyout>
                                <Flyout Placement="BottomEdgeAlignedRight">
                                    <StackPanel Spacing="8" MaxWidth="400">
                                        <TextBlock Text="{x:Bind ViewModel.PageHeaderTitle, Mode=OneWay}"
                                                       Style="{StaticResource BodyTextBlockStyle}"
                                                       TextWrapping="WrapWholeWords"
                                                       TextAlignment="Start"
                                                       IsTextSelectionEnabled="True"
                                                       Margin="0,0,0,4"/>

                                        <HyperlinkButton x:Uid="GuideButtonAtTop"
                                                             NavigateUri="{x:Bind ViewModel.PageHeaderGuideUri, Mode=OneWay}"
                                                             Visibility="{x:Bind ViewModel.GuideButtonVisibility, Mode=OneWay}">
                                            <HyperlinkButton.ContentTemplate>
                                                <DataTemplate x:DataType="x:String">
                                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                                        <FontIcon Glyph="&#xE7BE;" FontSize="16"/>
                                                        <TextBlock Text="{x:Bind}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </HyperlinkButton.ContentTemplate>
                                        </HyperlinkButton>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                    <!-- Inline header row (wide widths) -->
                    <controls:WrapPanel Grid.Row="1"
                                        Margin="20,10,0,0"
                                        Visibility="{x:Bind ViewModel.HeaderInlineVisibility, Mode=OneWay}"
                                        Style="{StaticResource PageHeaderWrapPanelStyle}">
                        <TextBlock Text="{x:Bind ViewModel.PageHeaderTitle, Mode=OneWay}"
                                   TextWrapping="WrapWholeWords"
                                   IsTextSelectionEnabled="True"
                                   Style="{StaticResource BodyTextBlockStyle}" />
                        <HyperlinkButton x:Uid="GuideButtonAtTop"
                                         NavigateUri="{x:Bind ViewModel.PageHeaderGuideUri, Mode=OneWay}"
                                         Visibility="{x:Bind ViewModel.GuideButtonVisibility, Mode=OneWay}">
                            <HyperlinkButton.ContentTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE7BE;" FontSize="16"/>
                                        <TextBlock Text="{x:Bind}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </HyperlinkButton.ContentTemplate>
                        </HyperlinkButton>

                    </controls:WrapPanel>

                    <!--  Note the use of a TwoWay binding here, this is required for this control to work.  -->
                    <CCToolKit:PropertySizer HorizontalAlignment="Left"
                                Grid.Row="2"
                                Binding="{x:Bind MainNavigation.OpenPaneLength, Mode=TwoWay}"
                                Maximum="640"
                                Minimum="60"
                                Visibility="{x:Bind MainNavigation.IsPaneOpen, Mode=OneWay}" />

                    <!-- Global margin settings for every page's content -->
                    <Frame x:Name="ContentFrame"
                           Margin="15,10,15,10"
                           Grid.Row="2"
                           AutomationProperties.LandmarkType="Main" />

                </Grid>
            </SplitView>

        </NavigationView>

    </Grid>
</Window>