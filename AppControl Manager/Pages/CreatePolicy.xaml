<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppControlManager.Pages.CreatePolicy"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customUI="using:AppControlManager.CustomUIElements"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    FlowDirection="{x:Bind ViewModel.AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}">

    <ScrollViewer>

        <StackPanel HorizontalAlignment="Stretch" Spacing="{StaticResource SettingsCardSpacing}">

            <win:StackPanel.ChildrenTransitions>
                <win:EntranceThemeTransition FromVerticalOffset="50" />
                <win:RepositionThemeTransition IsStaggeringEnabled="False" />
            </win:StackPanel.ChildrenTransitions>

            <!-- Allow Microsoft -->
            <controls:SettingsExpander
                x:Uid="AllowMicrosoftSettings"
                IsExpanded="{x:Bind ViewModel.AllowMicrosoftSettingsIsExpanded, Mode=TwoWay}">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xF0EF;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.AllowMSFTInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.AllowMSFTInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.AllowMSFTInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.AllowMSFTInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.AllowMicrosoftInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_AllowMicrosoft}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_AllowMicrosoft}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_AllowMicrosoft}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button x:Uid="AllowMicrosoftCreateButton"
                            Style="{StaticResource AccentButtonStyle}"
                            Margin="0,0,15,0"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}"
                            Click="{x:Bind ViewModel.AllowMicrosoftCreate_Click}" />

                    <ToggleButton IsChecked="{x:Bind ViewModel.AllowMicrosoftCreateAndDeploy, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.IsElevated}"
                                  x:Uid="DeployAfterCreationButton"
                                  Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="MakeAppIDTaggingPolicySettingsCard">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.AllowMicrosoftAppIDTaggingToggle, Mode=TwoWay}"
                                      x:Uid="ToggleSwitchGeneral"
                                      IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.AllowMicrosoftAudit, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <controls:SettingsCard x:Uid="LogSizeConfigSettingsCard" IsEnabled="{x:Bind ViewModel.IsElevated}">

                        <NumberBox
                            x:Uid="LogSizeNumberBox"
                            Value="{x:Bind ViewModel.EventLogsUtil.MaxSizeMB, Mode=TwoWay}"
                            SpinButtonPlacementMode="Inline"
                            SmallChange="1"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftLogSizeInputIsEnabled, Mode=OneWay}"
                            LargeChange="10"
                            Minimum="2"
                            Maximum="{x:Bind ViewModel.EventLogsUtil.MaxNumberBoxValue, Mode=OneTime}"
                            Margin="0,0,30,0" />

                    </controls:SettingsCard>

                    <customUI:SettingsCardV2 x:Uid="RequireEVSignersRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.AllowMicrosoftRequireEVSigners, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="EnableScriptEnforcementRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.AllowMicrosoftEnableScriptEnforcement, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="TestModeRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.AllowMicrosoftTestMode, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="NoMSRecommendedBlockRules">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.AllowMicrosoftNoBlockRules, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.AllowMicrosoftSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>

            </controls:SettingsExpander>

            <!-- Default Windows -->
            <controls:SettingsExpander
                x:Uid="DefaultWindowsSettings"
                IsExpanded="{x:Bind ViewModel.DefaultWindowsSettingsIsExpanded, Mode=TwoWay}">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEA18;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.DefaultWinInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.DefaultWinInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.DefaultWinInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.DefaultWinInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.DefaultWindowsInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_DefaultWindows}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_DefaultWindows}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_DefaultWindows}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}"
                        x:Uid="DefaultWindowsCreateButton"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        Click="{x:Bind ViewModel.DefaultWindowsCreate_Click}" />

                    <ToggleButton
                        IsChecked="{x:Bind ViewModel.DefaultWindowsCreateAndDeploy, Mode=TwoWay}"
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="MakeAppIDTaggingPolicySettingsCard">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.DefaultWindowsAppIDTaggingToggle, Mode=TwoWay}"
                                      x:Uid="ToggleSwitchGeneral"
                                      IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.DefaultWindowsAudit, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <controls:SettingsCard x:Uid="LogSizeConfigSettingsCard" IsEnabled="{x:Bind ViewModel.IsElevated}">

                        <NumberBox
                            x:Uid="LogSizeNumberBox"
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsLogSizeInputIsEnabled, Mode=OneWay}"
                            Value="{x:Bind ViewModel.EventLogsUtil.MaxSizeMB, Mode=TwoWay}"
                            SpinButtonPlacementMode="Inline"
                            SmallChange="1"
                            LargeChange="10"
                            Minimum="2"
                            Maximum="{x:Bind ViewModel.EventLogsUtil.MaxNumberBoxValue, Mode=OneTime}"
                            Margin="0,0,30,0" />

                    </controls:SettingsCard>

                    <customUI:SettingsCardV2 x:Uid="RequireEVSignersRuleOption">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DefaultWindowsRequireEVSigners,Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="EnableScriptEnforcementRuleOption">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DefaultWindowsEnableScriptEnforcement,Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="TestModeRuleOption">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DefaultWindowsTestMode,Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="NoMSRecommendedBlockRules">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DefaultWindowsSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DefaultWindowsNoBlockRules,Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>

            </controls:SettingsExpander>

            <!-- Signed and Reputable -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.SignedAndReputableSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="SignedAndReputableSettings">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEA0D;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.SignedAndRepInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.SignedAndRepInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.SignedAndRepInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.SignedAndRepInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.SignedAndReputableInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_SignedAndReputable}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_SignedAndReputable}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_SignedAndReputable}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}"
                        x:Name="SignedAndReputableCreate"
                        x:Uid="SignedAndReputableCreateButton"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        Click="{x:Bind ViewModel.SignedAndReputableCreate_Click}" />

                    <ToggleButton
                        IsChecked="{x:Bind ViewModel.SignedAndReputableCreateAndDeploy, Mode=TwoWay}"
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.SignedAndReputableAudit, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <controls:SettingsCard x:Uid="LogSizeConfigSettingsCard" IsEnabled="{x:Bind ViewModel.IsElevated}">

                        <NumberBox
                            x:Uid="LogSizeNumberBox"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableLogSizeInputIsEnabled, Mode=OneWay}"
                            Value="{x:Bind ViewModel.EventLogsUtil.MaxSizeMB, Mode=TwoWay}"
                            SpinButtonPlacementMode="Inline"
                            SmallChange="1"
                            LargeChange="10"
                            Minimum="2"
                            Maximum="{x:Bind ViewModel.EventLogsUtil.MaxNumberBoxValue, Mode=OneTime}"
                            Margin="0,0,30,0" />

                    </controls:SettingsCard>

                    <customUI:SettingsCardV2 x:Uid="RequireEVSignersRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.SignedAndReputableRequireEVSigners, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="EnableScriptEnforcementRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.SignedAndReputableEnableScriptEnforcement, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="TestModeRuleOption">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.SignedAndReputableTestMode, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="NoMSRecommendedBlockRules">
                        <ToggleSwitch
                            IsOn="{x:Bind ViewModel.SignedAndReputableNoBlockRules, Mode=TwoWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsEnabled="{x:Bind ViewModel.SignedAndReputableSectionIsEnabled, Mode=OneWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>

            </controls:SettingsExpander>

            <!-- Microsoft Recommended Driver Block Rules -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.RecommendedDriverBlockRulesSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="RecommendedDriverBlockRulesSettings">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEC1B;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.KernelModeBlockListInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.KernelModeBlockListInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.KernelModeBlockListInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.KernelModeBlockListInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.RecommendedDriverBlockRulesInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_RecommendedDriverBlockRules}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_RecommendedDriverBlockRules}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_RecommendedDriverBlockRules}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        x:Uid="RecommendedDriverBlockRulesCreateButton"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        IsEnabled="{x:Bind ViewModel.RecommendedDriverBlockRulesSectionIsEnabled, Mode=OneWay}"
                        Click="{x:Bind ViewModel.RecommendedDriverBlockRulesCreate_Click}" />

                    <ToggleButton
                        IsChecked="{x:Bind ViewModel.RecommendedDriverBlockRulesCreateAndDeploy, Mode=TwoWay}"
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                    <Button
                        x:Uid="RecommendedDriverBlockRulesScheduledAutoUpdate"
                        Style="{StaticResource AccentButtonStyle}"
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        Click="{x:Bind ViewModel.RecommendedDriverBlockRulesScheduledAutoUpdate_Click}"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>
                    <controls:SettingsCard x:Uid="DriversBlockRulesLittleSettingsCard">

                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                x:Uid="VersionLabel"
                                FontWeight="Bold"
                                Margin="0,0,5,0"
                                Foreground="Violet"/>
                            <TextBlock
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel._policyPathMSFTRecommendedDriverBlockRules.PolicyObj.VersionEx, Mode=OneWay, FallbackValue='N/A'}"
                                Foreground="Violet"/>
                        </StackPanel>

                    </controls:SettingsCard>
                </controls:SettingsExpander.Items>

            </controls:SettingsExpander>

            <!-- Microsoft Recommended User Mode Block Rules -->
            <controls:SettingsExpander
                x:Name="RecommendedUserModeBlockRulesSettings"
                x:Uid="RecommendedUserModeBlockRulesSettings"
                IsExpanded="{x:Bind ViewModel.RecommendedUserModeBlockRulesSettingsIsExpanded, Mode=TwoWay}">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEC1B;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.UserModeBlockListInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.UserModeBlockListInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.UserModeBlockListInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.UserModeBlockListInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.RecommendedUserModeBlockRulesInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_RecommendedUserModeBlockRules}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_RecommendedUserModeBlockRules}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_RecommendedUserModeBlockRules}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.RecommendedUserModeBlockRulesSectionIsEnabled, Mode=OneWay}"
                        x:Uid="RecommendedUserModeBlockRulesCreateButton"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        Click="{x:Bind ViewModel.RecommendedUserModeBlockRulesCreate_Click}" />

                    <ToggleButton
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        IsChecked="{x:Bind ViewModel.RecommendedUserModeBlockRulesCreateAndDeploy, Mode=TwoWay}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

            </controls:SettingsExpander>

            <!-- Strict Kernel-mode policy -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.StrictKernelModesSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="StrictKernelModePolicySection">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xE945;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        x:Uid="CurrentStatusInfoBar"
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.StrictKernelInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.StrictKernelInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.StrictKernelInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.StrictKernelInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.StrictKernelModeInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_StrictKernelModePolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_StrictKernelModePolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_StrictKernelModePolicy}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.StrictKernelModeSectionIsEnabled, Mode=OneWay}"
                        Click="{x:Bind ViewModel.StrictKernelModePolicyCreateButton_Click}"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        x:Uid="StrictKernelModePolicyCreateButton" />

                    <ToggleButton
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        IsChecked="{x:Bind ViewModel.StrictKernelModesCreateAndDeploy, Mode=TwoWay}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.StrictKernelModeSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.StrictKernelModesAudit, Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                    <customUI:SettingsCardV2 x:Uid="StrictKernelModePolicyUseNoFlightRootsToggleSwitchSettingsCard">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.StrictKernelModeSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.StrictKernelModeNoFlightRoots, Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>

            <!-- RMM Blocking -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.RMMBlockingSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="RMMBlockingSection">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEA14;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        x:Uid="CurrentStatusInfoBar"
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.RMMBlockingInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.RMMBlockingInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.RMMBlockingInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.RMMBlockingInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.RMMBlockingInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_RMMBlockingPolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_RMMBlockingPolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_RMMBlockingPolicy}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.RMMBlockingSectionIsEnabled, Mode=OneWay}"
                        Click="{x:Bind ViewModel.RMMBlockingPolicyCreateButton_Click}"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        x:Uid="RMMBlockingCreateButton" />

                    <ToggleButton
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        IsChecked="{x:Bind ViewModel.RMMBlockingCreateAndDeploy, Mode=TwoWay}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.RMMBlockingSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.RMMBlockingAudit, Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>

            <!-- Downloads Defense Measure -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.DownloadsDefenseMeasureSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="DownloadsDefenseMeasureSection">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEC9C;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        x:Uid="CurrentStatusInfoBar"
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.DownloadsDefenseMeasureInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.DownloadsDefenseMeasureInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.DownloadsDefenseMeasureInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.DownloadsDefenseMeasureInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.DownloadsDefenseMeasureInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_DownloadsDefenseMeasurePolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_DownloadsDefenseMeasurePolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_DownloadsDefenseMeasurePolicy}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.DownloadsDefenseMeasureSectionIsEnabled, Mode=OneWay}"
                        Click="{x:Bind ViewModel.DownloadsDefenseMeasurePolicyCreateButton_Click}"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        x:Uid="DownloadsDefenseMeasureCreateButton" />

                    <ToggleButton
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        IsChecked="{x:Bind ViewModel.DownloadsDefenseMeasureCreateAndDeploy, Mode=TwoWay}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DownloadsDefenseMeasureSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DownloadsDefenseMeasureAudit, Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>


            <!-- Dangerous Script Hosts Blocking -->
            <controls:SettingsExpander
                IsExpanded="{x:Bind ViewModel.DangerousScriptHostsBlockingSettingsIsExpanded, Mode=TwoWay}"
                x:Uid="DangerousScriptHostsBlockingSection">
                <controls:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xEA44;" />
                </controls:SettingsExpander.HeaderIcon>

                <controls:SettingsExpander.ItemsHeader>
                    <customUI:InfoBarV2
                        x:Uid="CurrentStatusInfoBar"
                        BorderThickness="0"
                        CornerRadius="0"
                        IsIconVisible="True"
                        Message="{x:Bind ViewModel.DangerousScriptHostsBlockingInfoBar.Message, Mode=OneWay}"
                        IsOpen="{x:Bind ViewModel.DangerousScriptHostsBlockingInfoBar.IsOpen, Mode=TwoWay}"
                        IsClosable="{x:Bind ViewModel.DangerousScriptHostsBlockingInfoBar.IsClosable, Mode=OneWay}"
                        Severity="{x:Bind ViewModel.DangerousScriptHostsBlockingInfoBar.Severity, Mode=OneWay}">
                        <customUI:InfoBarV2.ActionButton>
                            <DropDownButton
                                x:Uid="AdditionalActionsDropDownButton"
                                Visibility="{x:Bind ViewModel.DangerousScriptHostsBlockingInfoBarActionButtonVisibility, Mode=OneWay}">
                                <DropDownButton.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE712;" Foreground="Chartreuse" />
                                        <TextBlock x:Uid="AdditionalActionsTextBlock" />
                                    </StackPanel>
                                </DropDownButton.Content>
                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem
                                            x:Uid="OpenInPolicyEditorMenuFlyoutItem"
                                            Icon="Edit"
                                            Click="{x:Bind ViewModel.OpenInPolicyEditor_DangerousScriptHostsBlockingPolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenTheFileMenuFlyoutItem"
                                            Icon="OpenWith"
                                            Click="{x:Bind ViewModel.OpenInDefaultFileHandler_DangerousScriptHostsBlockingPolicy}"/>
                                        <MenuFlyoutItem
                                            x:Uid="OpenInConfigurePolicyRuleOptionsMenuFlyoutItem"
                                            Click="{x:Bind ViewModel.OpenInConfigurePolicyRuleOptions_DangerousScriptHostsBlockingPolicy}">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xEEA3;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </customUI:InfoBarV2.ActionButton>
                    </customUI:InfoBarV2>
                </controls:SettingsExpander.ItemsHeader>

                <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="6" VerticalSpacing="10">

                    <Button
                        IsEnabled="{x:Bind ViewModel.DangerousScriptHostsBlockingSectionIsEnabled, Mode=OneWay}"
                        Click="{x:Bind ViewModel.DangerousScriptHostsBlockingPolicyCreateButton_Click}"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,15,0"
                        x:Uid="DangerousScriptHostsBlockingCreateButton" />

                    <ToggleButton
                        IsEnabled="{x:Bind ViewModel.IsElevated}"
                        IsChecked="{x:Bind ViewModel.DangerousScriptHostsBlockingCreateAndDeploy, Mode=TwoWay}"
                        x:Uid="DeployAfterCreationButton"
                        Margin="0,0,15,0" />

                </controls:WrapPanel>

                <controls:SettingsExpander.Items>

                    <customUI:SettingsCardV2 x:Uid="AuditRuleOptionSettingsCard">
                        <ToggleSwitch
                            IsEnabled="{x:Bind ViewModel.DangerousScriptHostsBlockingSectionIsEnabled, Mode=OneWay}"
                            x:Uid="ToggleSwitchGeneral"
                            IsOn="{x:Bind ViewModel.DangerousScriptHostsBlockingAudit, Mode=TwoWay}" />
                    </customUI:SettingsCardV2>

                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>

        </StackPanel>
    </ScrollViewer>
</Page>