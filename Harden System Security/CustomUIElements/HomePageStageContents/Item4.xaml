<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AppControlManager.CustomUIElements.HomePageStageContents.Item4"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppControlManager.CustomUIElements.HomePageStageContents">

    <ScrollView>

        <Grid Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                   x:Uid="ComprehensiveAuditPoliciesManagement"
                   TextWrapping="Wrap"
                   FontSize="20"
                   Margin="0,0,0,4"
                   Foreground="#FFAADFFF"/>

            <TextBlock Grid.Row="1"
                   x:Uid="ComprehensiveAuditPoliciesManagementDescription"
                   FontSize="13"
                   TextWrapping="Wrap"
                   Foreground="#FFE1EEFA"
                   Margin="0,0,0,12"/>

            <Button Grid.Row="2"
                x:Uid="NavigateToAuditPoliciesPageButton"
                HorizontalAlignment="Left"
                FontSize="14"
                Click="{x:Bind Button_Click}"
                Padding="14,6"
                Background="#FF6BB7FF"
                Foreground="White"
                BorderThickness="0"
                CornerRadius="6"/>
        </Grid>

    </ScrollView>

</UserControl>
