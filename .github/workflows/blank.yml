name: released

on:
  release:
    types: [published]
  workflow_dispatch:
jobs:
  virustotal:
    runs-on: ubuntu-latest
    steps:
      -
        name: VirusTotal Scan
        uses: crazy-max/ghaction-virustotal@v3
        with:
          vt_api_key: ${{ secrets.VTAPI }}
          files: |
            .\GroupPolicy\**
